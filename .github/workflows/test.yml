name: "units-test"
on:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

jobs:
  # unit tests
  units:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: npm ci
    - run: npm test

  # test action works running from the graph
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: query=nginx
      id: test-1
      uses: ./
      with:
        query: nginx
    - name: query=nginx, limit=10
      id: test-2
      uses: ./
      with:
        query: nginx
        limit: 10
    - name: query=nginx, date=2022
      id: test-3
      uses: ./
      with:
        query: nginx
        date: 2022
    - name: query=nginx, version=1.22
      id: test-4
      uses: ./
      with:
        query: nginx
        version: 1.22
    - name: query=nginx, date=2022, version=1.22
      id: test-5
      uses: ./
      with:
        query: nginx
        date: 2022
        version: 1.22
    outputs:
      test-1: ${{ steps.test-1.outputs.matrix }}
      test-2: ${{ steps.test-2.outputs.matrix }}
      test-3: ${{ steps.test-3.outputs.matrix }}
      test-4: ${{ steps.test-4.outputs.matrix }}
      test-5: ${{ steps.test-5.outputs.matrix }}
